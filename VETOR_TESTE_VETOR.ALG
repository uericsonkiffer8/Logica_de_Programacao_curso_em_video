algoritmo "VETOR_TESTE_vetor"

var
   v: vetor[1..6] de inteiro
   c: INTEIRO
inicio
   Para c <- 1 ate 6 faca
        Escreva("Digite o ",c , " o. valor: " )
        leia(V[C])
   FimPara
      Para c <- 1 ate 6 faca
           escreva("{", v[c], "}")
      FimPara
fimalgoritmo